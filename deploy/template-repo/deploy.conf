# Deploy configuration (all fields optional)
# Delete this file to use defaults (subdomain = repo name, port = 8080)

# subdomain = my-app          # Override subdomain (default: repo name)
# container_port = 8080       # Port your app listens on inside the container
# health_check = /health      # Wait for this endpoint before marking deploy complete
# env = DATABASE_URL           # Environment variables (from .env on server)
